라이브 추론 템플릿 사용법
- 각 CSV는 48행(window size)입니다.
- 컬럼명은 타겟별 추천 피처와 일치합니다.
- 값은 모델 학습 시 저장된 X_scaler의 평균/표준편차를 기반으로 생성했습니다.
- 시간 피처(hour_sin/cos, weekday 등)는 30분 간격 24시간 패턴으로 채웠습니다.
- 추론 탭에서 파일 업로드 후 바로 실행하면 됩니다.
